# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local overrides — never commit. backend/.gitignore already ignores its own .env;
# frontend's was missing, so frontend/.env showed up as untracked and could be swept
# into a `git add -A`. The checked-in .env.development / .env.production stay tracked.
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
