# Production API URL - empty string = relative URLs (same-origin nginx proxy)
# Frontend and backend are served from the same host via nginx, so relative paths work
# perfectly and avoid all CORS issues regardless of HTTP/HTTPS.
VITE_API_BASE_URL=
VITE_APP_NAME=Panel Pulse AI
VITE_ENABLE_MOCK=false
